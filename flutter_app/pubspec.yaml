name: emo_parent_dashboard
description: EMO Family Internet Manager Parent Dashboard
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.16.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  mqtt_client: ^10.0.0
  provider: ^6.1.0
  flutter_secure_storage: ^9.0.0
  fl_chart: ^0.68.0
  firebase_messaging: ^15.0.0
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  json_serializable: ^6.8.0
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
